name: Auto Healing Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
        
    - name: Detect problems and auto-heal
      id: heal
      run: |
        echo "🔍 Starting Auto-Healing Diagnostic Scan..."
        
        # Initialize healing flag
        NEEDS_COMMIT="false"
        
        # Healing Layer 1: Remove problematic files
        if [ -f "broken.txt" ]; then
          echo "🚑 HEALING: Removing broken.txt file"
          rm broken.txt
          NEEDS_COMMIT="true"
        fi
        
        # Healing Layer 2: Fix Python syntax issues
        if [ -f "src/app.py" ]; then
          echo "🔧 Checking Python code health..."
          if python -m py_compile src/app.py; then
            echo "✅ Python syntax is valid"
          else
            echo "🚑 HEALING: Fixing Python syntax issues"
            # Fix common Python errors
            sed -i 's/printx(/print(/g' src/app.py 2>/dev/null || true
            sed -i 's/print (/print(/g' src/app.py 2>/dev/null || true
            NEEDS_COMMIT="true"
          fi
        fi
        
        # Healing Layer 3: Fix missing requirements
        if [ ! -f "requirements.txt" ]; then
          echo "🚑 HEALING: Creating missing requirements.txt"
          echo "flask==2.3.3" > requirements.txt
          echo "requests==2.31.0" >> requirements.txt
          NEEDS_COMMIT="true"
        fi
        
        # Set output if healing occurred
        if [ "$NEEDS_COMMIT" = "true" ]; then
          echo "needs_commit=true" >> $GITHUB_OUTPUT
          echo "💊 Auto-healing treatments applied"
        else
          echo "✅ No healing needed - system is healthy"
        fi
        
    - name: Commit fixes if needed
      if: steps.heal.outputs.needs_commit == 'true'
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add .
        git commit -m "🤖 Auto-heal: Fixed issues detected in pipeline"
        git push
        
    - name: Install dependencies
      run: |
        if [ -f "requirements.txt" ]; then
          pip install -r requirements.txt
        fi
        
    - name: Run tests
      run: |
        echo "🧪 Running test suite..."
        # Windows-friendly: no chmod needed
        bash tests/test_script.sh
        
    - name: Final status
      run: |
        echo "🎉 PIPELINE STATUS: SUCCESS"
        echo "💚 System is healthy and operational"
        echo "🤖 Auto-healing: ACTIVE"
        echo "📊 All validation checks: PASSED"